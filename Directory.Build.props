<Project>
    <PropertyGroup>
        <!-- summary is not migrated from project.json, but you can use the <Description> property for that if needed. -->
        <PackageTags>f#, fsharp</PackageTags>
        <PackageProjectUrl>https://github.com/TeX-host/TeXFormulaLayout</PackageProjectUrl>
        <PackageLicenseUrl>https://github.com/TeX-host/TeXFormulaLayout/blob/master/LICENSE.md</PackageLicenseUrl>
        <PackageRequireLicenseAcceptance>false</PackageRequireLicenseAcceptance>
        <RepositoryType>git</RepositoryType>
        <Authors>TeX-host</Authors>
        <RepositoryUrl>https://github.com/TeX-host/TeXFormulaLayout</RepositoryUrl>
        <!-- owners is not supported in MSBuild -->
    </PropertyGroup>
</Project>
